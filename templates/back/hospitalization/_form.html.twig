{{ form_start(formHospitalization) }}
<div class="overflow-hidden shadow sm:rounded-md">
    <div class="bg-white px-4 py-5 sm:p-6">
        <div class="grid grid-cols-6 gap-6">
            <div class="col-span-6">
                <div class="px-4 mt-10 sm:px-0">
                    <p class="font-medium text-gray-900">Choisir l'utilisateur</p>
                </div>
                <div class="block" aria-hidden="true">
                    <div class="py-5">
                        <div class="border-t border-gray-200"></div>
                    </div>
                </div>
                <div class="text-red-500 font-bold my-2">{{ form_errors(formHospitalization.user_integer) }}</div>
                {{ form_label(formHospitalization.user_integer) }}
                {{ form_widget(formHospitalization.user_integer) }}
            </div>
            <div class="col-span-6">
                <div class="px-4 mt-10 sm:px-0">
                    <p class="font-medium text-gray-900">Choisir la pathologie d'opération</p>
                </div>
                <div class="block" aria-hidden="true">
                    <div class="py-5">
                        <div class="border-t border-gray-200"></div>
                    </div>
                </div>
                <div class="text-red-500 font-bold my-2">{{ form_errors(formHospitalization.pathology_integer) }}</div>
                {{ form_label(formHospitalization.pathology_integer) }}
                {{ form_widget(formHospitalization.pathology_integer) }}
            </div>
            <div class="col-span-6">
                <div class="text-red-500 font-bold my-2">{{ form_errors(formHospitalization.entry_date) }}</div>
                {{ form_label(formHospitalization.entry_date) }}
                {{ form_widget(formHospitalization.entry_date) }}
            </div>

            <div class="col-span-6">
                <div class="text-red-500 font-bold my-2">{{ form_errors(formHospitalization.end_date) }}</div>
                {{ form_label(formHospitalization.end_date) }}
                {{ form_widget(formHospitalization.end_date) }}
            </div>
        </div>
    </div>
    <div class="bg-gray-50 px-4 py-3 text-right sm:px-6">
        <button class="inline-flex justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">{{ button_label|default('Enregistrer') }}</button>
    </div>
</div>
{{ form_end(formHospitalization) }}
