<form method="post" action="{{ path('back_app_gift_delete', {'id': gift.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gift.id) }}">
    <button class="btn delete-button">Delete</button>
</form>
