{% extends 'base_front.html.twig' %}

{% block title %}Modification cadeau{% endblock %}

{% block body %}
    <div class="gift-form">

        <div class="container-form">
            <h1>Modifier un cadeau</h1>

            {{ include('back/gift/_form.html.twig', {'button_label': 'Update'}) }}

            <div class="container-action">

                <a href="{{ path('back_app_gift_index') }}" class="back-button">Retour</a>

                {{ include('back/gift/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
