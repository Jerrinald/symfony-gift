<div class="form-content">
    {{ form_start(form, {'class': 'gift-form'}) }}
        {{ form_widget(form) }}
        <button class="btn action">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
</div>